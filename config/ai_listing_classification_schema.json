{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "normalized_product_name": {"type": ["string", "null"]},
    "condition": {
      "type": "string",
      "enum": ["normal", "risky", "broken", "unknown"]
    },
    "confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "reject": {"type": "boolean"},
    "reason": {"type": "string", "maxLength": 240},
    "scope": {
      "type": "string",
      "enum": ["standalone", "bundle", "complete_pc", "accessory", "unknown"]
    }
    ,"sale_status": {
      "type": "string",
      "enum": ["active", "reserved", "sold", "unavailable", "unknown"]
    },
    "usable_for_market_price": {"type": "boolean"}
  },
  "required": [
    "normalized_product_name",
    "condition",
    "confidence",
    "reject",
    "reason",
    "scope",
    "sale_status",
    "usable_for_market_price"
  ]
}
